apiVersion: v1
kind: Service
metadata:
  name: {{ include "ephemera.fullname" . }}
  labels:
    {{- include "ephemera.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ephemera.selectorLabels" . | nindent 4 }}
