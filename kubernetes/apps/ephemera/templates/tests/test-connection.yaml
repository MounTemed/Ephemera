apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ephemera.fullname" . }}-test-connection"
  labels:
    {{- include "ephemera.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ephemera.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
